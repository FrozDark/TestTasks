<dialogs:DialogBase
    x:Class="TestTasks.DialogViews.ConfirmDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dialogs="clr-namespace:TestTasks.DialogViews"
    xmlns:local="clr-namespace:TestTasks.DialogViews"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:TestModels;assembly=TestModels"
    xmlns:templates="clr-namespace:TestTasks.DataTemplates"
    xmlns:vm="clr-namespace:TestViewModels;assembly=TestViewModels"
    xmlns:vmd="clr-namespace:TestViewModels.Dialogs;assembly=TestViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <d:DesignData.DataContext>
        <vmd:ConfirmDialogViewModel />
    </d:DesignData.DataContext>

    <dialogs:DialogBase.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding ConfirmCommand, Mode=OneTime}" />
        <KeyBinding Key="Esc" Command="{Binding CancelCommand, Mode=OneTime}" />
    </dialogs:DialogBase.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="18"
            FontWeight="Bold"
            Text="{Binding Title, Mode=OneTime}" />
        <ScrollViewer
            Grid.Row="1"
            Margin="0,10,0,0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <TextBlock Text="{Binding Message, Mode=OneTime}" TextWrapping="WrapWithOverflow" />
        </ScrollViewer>

        <Border
            Grid.Row="2"
            Height="10"
            Margin="0,5,0,5"
            BorderBrush="Gray"
            BorderThickness="0,0,0,1" />

        <StackPanel
            Grid.Row="3"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="3,0,3,0" />
                    <Setter Property="Padding" Value="5,3" />
                    <Setter Property="MinWidth" Value="40" />
                </Style>
            </StackPanel.Resources>
            <Button
                x:Name="btnClose"
                Command="{Binding CloseCommand, Mode=OneTime}"
                Content="Закрыть"
                Visibility="{Binding HasCloseButton, Mode=OneTime, Converter={StaticResource BoolToVisibilityConverter}}" />
            <Button
                x:Name="btnCancel"
                Command="{Binding CancelCommand, Mode=OneTime}"
                Content="Нет"
                IsCancel="True"
                Visibility="{Binding HasCancelButton, Mode=OneTime, Converter={StaticResource BoolToVisibilityConverter}}" />
            <Button
                x:Name="btnConfirm"
                Command="{Binding ConfirmCommand, Mode=OneTime}"
                Content="Да"
                IsDefault="True"
                Visibility="{Binding HasConfirmButton, Mode=OneTime, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</dialogs:DialogBase>
