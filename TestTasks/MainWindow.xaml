<Window
    x:Class="TestTasks.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TestTasks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewsd="clr-namespace:TestTasks.DialogViews"
    xmlns:vm="clr-namespace:TestViewModels;assembly=TestViewModels"
    xmlns:vmd="clr-namespace:TestViewModels.Dialogs;assembly=TestViewModels"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Frame Content="{Binding CurrentPage}" />

        <Grid DataContext="{Binding CurrentPage.Dialogs}" Visibility="{Binding Count, Converter={StaticResource IsNotNullZeroOrEmptyConverter}, FallbackValue=Hidden}">
            <Rectangle
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Fill="Black"
                Opacity="0.7" />
            <ContentControl
                x:Name="dialogFrame"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Content="{Binding CurrentDialog}" />
        </Grid>
    </Grid>
</Window>
